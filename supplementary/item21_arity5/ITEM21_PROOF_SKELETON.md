# Item 21 — a proof skeleton (XXIII-level), reducing the exotic to two FFT lemmas

*Paper-math attempt (2026-06-23) at direction (a): assemble the session's verified pieces +
item 23 + Paper XIX/XXII into a structural argument for item 21, reducing the unparametrized
"exotic arity-5" problem (O3) to named first-fundamental-theorem (FFT) targets, split by an
`ω`/radical dichotomy. **Firewall tags:** `[VERIFIED]` (computed this session), `[PROVEN mod X]`
(established elsewhere, dependency named), `[GAP]`, `[REFUTED]` (a sub-conjecture tested and broken).
This is a skeleton, not a finished proof — and one of its sub-lemmas (Lemma B) was refuted by its own
verification, which sharpened the target.*

## Target (operative, cochain-level)

> **Theorem (item 21, target).** For `n≥5`, no *exotic* (indecomposable, non-`ω`-cup-power) arity-5
> invariant of Lagrangian 5-tuples in proper position furnishes a nonzero `H⁴` obstruction at `K₆`.
> Equivalently, `H³` is the hard ceiling: the family-A tower (`μ→n_a`) does not gain an arity-5 rung.

The qualifier *exotic / indecomposable* is essential and is the lesson of the `h4_cohomology` negative:
"every arity-5 invariant is exact" is **false** (orbit-indicators give `Σ_m c_m≠0`), so item 21 is about
the **natural relative-position data**, not arbitrary `Sp`-invariant functions.

## Step 0 — the data is split (the localization) `[VERIFIED]`

Completeness lemma + `R=ker(G)⟺rad=0` (`kerG_reduction.py`, 1100/1100): every arity-5 invariant is a
function of `(G,R)`, and the **only datum beyond the `ω`-Gram `G` is `rad(ω|_W)`** — present exactly on
degenerate strata (`n≥5`), and equal to **Paper XIX's modulus order parameter**. (`n=4`: 100%
nondegenerate ⟹ no extra datum ⟹ no modulus, consistent with `n=4` settled.) So the natural arity-5
data is **(I) the `ω`-Gram data + (II) the radical/modulus data**, and item 21 splits accordingly.

## Step 1 — the `ω`-part has no indecomposable `H⁴`

The correct grading for `ω`-data is **cohomological degree** (cup product raises degree), *not*
polynomial degree on the nerve (the error in an earlier draft). The natural `ω`-data is the
**cup-Steenrod subring generated by `ω`** inside `H*(V;F₂)` (`V=(ℤ/2)^{2n}`):

`H²: ω` ; `H³: Sq¹ω = n_a` ; `H⁴: ω²=Sq²ω` (decomposable) , `Sq¹Sq¹ω=0` ; `H^{>4}`: higher cup powers.

- **`[PROVEN mod Kudo]` (item 23 / Paper XXII).** The only indecomposable Steenrod lift of `ω` is
  `Sq¹ω` (`H³`); `Sq¹Sq¹ω=0` (Adem); `Sq²ω=ω²` is **decomposable** (a cup power = family B); `Sq^iω=0`,
  `i>2`. So the `ω`-subring has **no indecomposable `H⁴` class** — its only `H⁴` class is `ω²` (family B,
  the known cup-power floor, *not* a new arity-5 obstruction).
- **`[VERIFIED n=3,4 | CLASSICAL]` Lemma A payoff — corrected to the ORTHOGONAL ring
  (`sp_invariants.py`, `LEMMA_A_README.md`).** Two corrections: (i) the framework's `ω=[c]` is the
  **quadratic form `q`** (`q(v)=c(v,v)`), invariant under **`O`**, not `Sp` (`dim H²^{Sp}=0`,
  `dim H²^{O}=1=q`); so the obstruction `n_a=Sq¹q` lives in `H*(BV)^{O}`. (ii) A kernel-computation bug
  was caught (direct check: `q,Sq¹q` are `O`-fixed) and fixed. **Result:** `dim H⁴(BV)^{O}=1` (just
  `q²`, decomposable) at `n=3,4` — the orthogonal generators sit at degrees `2,3,5,9,… = {2}∪{1+2^i}`,
  **skipping degree 4**. So **no indecomposable degree-4 `O`-invariant ⟹ no exotic arity-5/`H⁴`
  obstruction from the `ω`/`q`-part**, and the `H³` ceiling is the orthogonal degree-gap `3→5`. The
  remaining `[GAP]` is the **bridge**: that the genuine config obstructions = ambient `O`-classes (the
  natural-data identification = item 23's framework extended) — which also *resolves* the over-counting
  (config indicators/weight-enumerators over-count precisely because they are NOT ambient `O`-classes).

**Conclusion of Step 1 (VERIFIED payoff + bridge gap):** the genuine `ω`/`q`-part obstructions are the
ambient `O`-invariant Steenrod classes, which have **no indecomposable `H⁴`** (`q²` only; verified
`n=3,4`, classical `n≥3`). Modulo the ambient↔config bridge, the `ω`-part of item 21 is closed.

## Step 2 — the radical/modulus part is governed by Arf

The non-`ω` data lives on `rad(ω|_W)` (degenerate strata, `n≥5`) = XIX's modulus. The natural non-`ω`
invariant is the **Arf invariant of the induced quadratic refinement** `Q` on the relevant quadratic
space.

- **`[PROVEN, classical]`** Over `F₂`, a nondegenerate quadratic form on `F₂^{2m}` is determined up to
  isometry by its **Arf invariant** (one bit) — Arf is the complete invariant of an *abstract* `F₂`
  quadratic space.
- **`[REFUTED — VERIFIED]` Lemma B (the radical-FFT, as originally stated).** I conjectured the non-`ω`
  radical invariants are *generated by Arf* (i.e. `(G,Arf)` complete on degenerate strata). **This is
  false** (`lemmaB_test.py`): among degenerate 5-configs there are distinct orbits sharing `(canon-G,
  Arf)` but differing in `R` — **15 collisions at `n=5`, 56 at `n=6`** (every `canon-G` hosting `>1`
  orbit also collides under `(G,Arf)`). Reason: `Arf≈0` (M4) is *nearly constant*, so it cannot separate
  the radical freedom; the genuine extra datum is the **position of `R` inside `ker(G)`** (the radical's
  subspace structure — a Grassmannian-type modulus invariant), of which Arf is only a sliver. So the
  radical/modulus carries **strictly more invariants than Arf** — these are exactly **Paper XIX's
  modulus** invariants (richer than the single Arf bit). *(The classical Arf-completeness is for the
  abstract quadratic space; it does NOT capture how the radical sits inside `W` — that extra structure
  is the modulus.)*
- **`[VERIFIED value-level | GAP cochain-level]` (M4, `m4_cochain.py`).** The intrinsic arity-5
  `Arf(Q₅)` is `≈ const 0` where defined (XIX-consistent) — so the **Arf sliver** of the radical data
  does not climb (value-level). But Arf is only a sliver (Lemma B refuted), so this closes only part of
  Step 2.

**Conclusion of Step 2 (corrected):** the radical part is **richer than Arf** — it is the full XIX
modulus invariant theory. M4 handles the Arf sliver (`≈0`); the genuine open gap is the **radical/
modulus FFT** (classify the modulus invariants) **and** showing *none* of them climb to `H⁴`.

**Modulus FFT — two further findings (2026-06-23):**
- **Primary generator identified `[VERIFIED]` (`modulus_fine.py`):** the **weight enumerator of `R`**
  (the relation space as a binary code in `F₂^{10}`) is the *dominant* fine modulus generator —
  adding it collapses the `(canon-G, Arf)` collisions `17→2` (`n=5`), `80→11` (`n=6`), and **Arf is
  subsumed** (`(canon-G, weight-enum R)` alone gives the same residual; Arf `≈0` contributes nothing).
  A small residual remains → at least one more generator. So the modulus generators are
  `{N_anti (from G), weight-enum(R), …}`, *not* Arf.
- **No-climb test over-counts `[VERIFIED]` (`modulus_climb.py`):** the weight-enumerator components
  `A_w = (#`weight-`w` codewords in `R)` mod 2 give `Σ_m A_w(face m) ≠ 0` for `w=4..10` at `n≥5`. **This
  is NOT an escape** — it is the `h4_cohomology` over-counting trap: `A_w` is a *non-coboundary*
  invariant that varies across orbits, so it pairs `≠0` generically (the 6 faces are generically
  distinct orbits), exactly like an orbit-indicator. `N_anti` pairs to 0 *only* because it is an exact
  coboundary (M1); `A_w` is not. **Consequence:** the naive `Σ_m=0` test flags *all* non-coboundary
  modulus invariants, natural or not — so the modulus no-climb theorem **cannot be computational**; it
  requires the natural/indecomposable-data delimitation. The over-counting wall is reached *from inside
  the modulus* — confirming (now a 3rd independent time) that the no-climb is insight-bound.

This is larger than the original "Arf complete" claim — and the firewalls (testing Lemma B, then
recognizing the over-counting in the climb test) caught two over-simplifications.

## Step 1 ∪ Step 2 UNIFIED by the ambient-`O`-class criterion — Step 2 dissolves

Lemma A supplied a concrete criterion for "natural data": **a genuine arity-5/`H⁴` obstruction is the
evaluation on the nerve of an ambient `O`-invariant degree-4 cohomology class** (the over-counting
config-invariants — indicators, weight enumerators — are excluded precisely because they are *not*
ambient `O`-classes). This criterion **dissolves Step 2's hard core (the modulus FFT)**:

- The genuine obstructions of **both** parts (`ω`-Gram *and* radical/modulus) are ambient `O`-classes.
- `[VERIFIED n=3,4,5]` `dim H⁴(BV)^{O} = 1`, spanned by `q²` alone (`sp_invariants.py`).
- `q²` is **decomposable** (a cup-square = family B) — *not* a new/primary obstruction (item 22:
  family B is the floor, no independent resonance).
- ⟹ **there is no indecomposable/primary degree-4 ambient `O`-class, so no exotic arity-5/`H⁴`
  obstruction from any source.** The modulus config-invariants that "climb" (`modulus_climb.py`) are
  over-counting artifacts — not ambient `O`-classes, hence not genuine obstructions — so they need not
  (and cannot) be classified for item 21. The modulus FFT is moot for the `H⁴` question.

So item 21 (`n≥5`) reduces to a **single** remaining gap: the **D-bridge** — *every* genuine
contextuality obstruction is the nerve-evaluation of an ambient `O`-invariant class. This is **Direction
D** (the spectral-sequence / characteristic-class bridge; memory `project_paper22`), with **item 23**
(`n_a = Sq¹q`, mod Kudo) as its *verified degree-3 instance*. The D-bridge is general (all degrees), so
once established it gives item 21 for free from the finite computed fact `dim H⁴^O = 1`. *(Caveat: the
D-bridge is a substantial program, not a corollary; but it is a **named, general** target, replacing the
unparametrized O3 exotic search.)*

**→ The D-bridge is now stated generally and its load-bearing piece verified — see `DBRIDGE.md`.**
Three things settled there: **(1)** item 21 needs the *classification* direction (config ⟹ ambient),
**not** item 23's *realization* direction (ambient ⟹ config) — and classification is exactly what
excludes the over-counters. **(2)** General form = the LHS spectral sequence of the extra-special
extension `1→ℤ/2→H→V→1`: `E₂=H*(V)[t]` (Quillen), obstructions = Kudo transgressions
`τ(t^{2^k})=Sq^{2^{k-1}}…Sq¹q` at degrees `{2}∪{1+2^i}` — **the degree-4 gap is structural** (`t²→`deg3,
`t⁴→`deg5). `bridge_tower.py` `[VERIFIED n=2,3,4]`: the ambient generators *are* the tower (not just
dimension-matched) through degree 5 — `Sq²Sq¹q` is the indecomposable degree-5 generator; degree 4 has
only `q²=Sq²q` (decomposable) and `Sq¹Sq¹q=0` (Adem). **(3)** The one new piece is a `[REFRAME]`: a
*genuine* obstruction = a class pulled from `H*(BH)` (a characteristic class of the operator structure),
not an arbitrary `Sp`-invariant config function — justified because contextuality is a property of the
operator algebra `H`, not of a Lagrangian-tuple presentation. Under the reframe, item 21 is structurally
**lighter** than item 23: being a *negative* statement it needs only "obstructions can't exceed the
ambient ring" + the verified decomposability of `H⁴(BV)^O`, not item 23's hard *construction* of a class.

## Assembled statement

> **Item 21 holds for `n≥5`** *modulo*: **(i)** Lemma A — payoff **VERIFIED** (`n=3,4`: no
> indecomposable degree-4 `O`-invariant; orthogonal generators at `{2,3,5,9,…}` skip degree 4) modulo
> the **ambient↔config bridge** (= item 23's framework extended); **(ii)** item 23 (Steenrod truncation,
> mod Kudo) — subsumed by (i)'s `O`-ring computation; **(iii′)** the **radical/modulus FFT** — classify
> the modulus invariants (Arf is only a sliver; Lemma B's "Arf complete" is **refuted**); **(iv)** show
> *none* of the modulus invariants climb to `H⁴` (M4 closes the Arf sliver value-level; the naive test
> over-counts, so this needs the natural-data delimitation — *which the bridge in (i) supplies*).

This is the corrected content of M5. The `ω`-part (Step 1) is in good shape: it reduces to the char-2
C-O generation theorem (Lemma A) plus the already-established Steenrod truncation (item 23). The
**radical part (Step 2) is the genuine hard core** and is *larger* than first hoped — it is the full
**Paper XIX modulus invariant theory** (the position of `R` in `ker G`), not just Arf. So item 21's
true remaining content is: *the char-2 generation theorem for the radical/modulus data, plus a ceiling
(no-climb) theorem for it* — the radical analogue of XXII applied to all modulus invariants, not only
`n_a` and Arf. The unparametrized O3 search is replaced by this structured (still hard) target, and the
`ω` / radical dichotomy (Step 0) is what makes the split possible.

## `[TODO]` — the next steps (firewall: support GAPs before trusting them)
1. **Lemma B test — DONE (`lemmaB_test.py`): REFUTED.** `(G,Arf)` is *not* complete on the degenerate
   strata (15/56 collisions at `n=5/6`); the radical carries modulus invariants beyond Arf. So Step 2's
   target is the full modulus FFT, not "Arf complete."
2. **Characterize the radical/modulus invariants** — PARTLY DONE (`modulus_fine.py`): the **weight
   enumerator of `R`** is the primary generator (Arf subsumed), with a small residual generator(s) still
   to name. Full classification = XIX's modulus invariant ring (only partial in XIX) — the hard core.
3. **No-climb for the modulus invariants — the naive test is INADEQUATE (`modulus_climb.py`):** the
   modulus generators over-count (`Σ_m A_w ≠ 0` for non-coboundary `A_w`, the `h4_cohomology` trap), so
   no-climb cannot be a `Σ_m=0` computation. It needs the **natural/indecomposable-data delimitation**
   (which invariants are genuine contextuality data) — the insight-bound crux, now confirmed a 3rd time
   (after `h4_cohomology` and the kerG self-correction). This is *the* obstacle to a computational
   close of item 21.
4. **Lemma A:** make the `ω`-Gram-invariants ↔ `ω`-cup-Steenrod-subring correspondence precise (the
   char-2 C-O generation theorem for the `ω`-part).

## Files
- `kerG_reduction.py` / `M5_kerG_reduction_README.md` — Step 0 (the data split).
- `m4_cochain.py` / `M4_intrinsic_README.md` — Step 2 (Arf `≈0`, corrected).
- `sp_invariants.py` / `LEMMA_A_README.md` — Step 1 (Lemma A payoff VERIFIED: `dim H⁴(BV)^{O}=1=⟨q²⟩`
  at `n=3,4`; orthogonal generators skip degree 4; group corrected `Sp`→`O`, kernel bug fixed).
- `lemmaB_test.py` — Step 2 (Lemma B test: `(G,Arf)` NOT complete on degen strata → Lemma B refuted).
- `co_reduce.py` / `LITERATURE.md` — the C-O connection (Lemma A's prior art).
- item 23: `../item23_search/closing_note.md` (Steenrod truncation, mod Kudo).
